[build]
builder = "NIXPACKS"
buildCommand = "cd apps/backend && npm install && npx medusa build"

[deploy]
startCommand = "cd apps/backend && npx medusa migrations run && npx medusa start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
